import React, { Component } from 'react';

export default class Search extends Component {
  render() {
    return (
      <section>
        <button className="sort-up">Sort Up</button>
        <button className="sort-down">Sort Down</button>
      </section>
    )
  }
}
